-- 060: "ครั้งที่" ของงานในเคลม เก็บชัด ๆ (13/09/69)
--
-- เดิมเว็บนับครั้งที่จากลำดับที่เคสของเลขเคลมเดียวกันถูกสร้างในระบบเรา (ROW_NUMBER ตาม created_at)
-- ซึ่งไม่ตรง EMCS ทันทีที่ครั้งก่อนหน้าไม่ได้อยู่ในเว็บครบ (ดึงมาเฉพาะบางใบ) — ตัวดึงงานจาก ISURVEY
-- คำนวณครั้งที่จากเลขเรื่องเซอร์เวย์ทุกใบของเคลม (autokey/survey_order.py: ปีเดือน → ลำดับเรื่อง
-- ตรวจกับ EMCS จริงแล้ว) แล้วส่งมาเก็บที่นี่ · NULL = นับจากลำดับสร้างเหมือนเดิม (งานจากแอป/ไฟล์ XML)
--
-- งานอ้างอิง (source = 'isurvey_reference') = ครั้งก่อนหน้าของเคลมที่ปิดจบบน ISURVEY แล้ว
-- ตัวดึงงานเอามาให้เห็นประวัติครบเหมือน EMCS: อนุมัติแล้ว/ถือว่าเข้า EMCS แล้ว/ปิด ISURVEY แล้ว ตั้งแต่สร้าง
-- (source เป็น VARCHAR(20) อยู่แล้ว ไม่ต้องแก้ตาราง)
--
-- รันด้วยมือบน production: psql "$DATABASE_URL" -f 060_visit_no.sql

ALTER TABLE cases ADD COLUMN IF NOT EXISTS visit_no INT;

COMMENT ON COLUMN cases.visit_no IS
  'ครั้งที่ของงานในเคลม ตามเลขเรื่องเซอร์เวย์ (ตัวดึงงาน ISURVEY คำนวณให้) · NULL = นับจากลำดับสร้าง';
